{
  plugins.which-key = {
    enable = true;

    registrations = {
      "<leader>f" = "Telescope";
      "<leader>g" = "Git Signs";
    };
  };

  plugins.mini = {
    enable = true;

    modules = {
      icons = {};
    };
  };
}
